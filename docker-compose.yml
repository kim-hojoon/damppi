name: damppi

services:
  mosquitto:
    image: eclipse-mosquitto:2.0
    restart: unless-stopped
    ports:
      - "1883:1883"
      - "8080:80"
    volumes:
      - "./mosquitto.conf:/mosquitto/config/mosquitto.conf"
